# 🔄 Sorting

> ⏱ 1 min revision

---

## ✨ Arrays

```java
Arrays.sort(arr);
```

## ✨ ArrayList

```java
Collections.sort(list);
Collections.reverse(list);
```

## ⭐ Remember

- Arrays → `Arrays.sort()`
- List → `Collections.sort()`
